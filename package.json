{
  "name": "js-reporters",
  "version": "1.0.0",
  "scripts": {
    "build": "esperanto -b -i lib/index.js -t umd --name JsReporters | babel -o dist/js-reporters.js --blacklist strict -m umd"
  },
  "devDependencies": {
    "babel": "^5.6.14",
    "bower": "^1.4.1",
    "esperanto": "^0.7.3"
  }
}
